import './Header.css';

const Header = () => {
    return (
        <header className = "header">
            <p className = "header__text">React</p>
        </header>
    );
}

export default Header;