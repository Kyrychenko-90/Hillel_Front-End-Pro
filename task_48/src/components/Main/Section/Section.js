import './Section.css'

const Section =() => {
    return (
        <section className = "box">
            <h1 className = "box__title">This is React</h1>
            <p className = "box__text">This page is an overview of the React documentation and related resources.</p>
        </section>
    );
}

export default Section;